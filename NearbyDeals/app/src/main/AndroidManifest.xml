<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zaks.ayala.nearbydeals" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".ui.SettingActivity"
            android:label="@string/preferences_activity_label"
            android:parentActivityName=".ui.DealsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name=".data.contentproviders.CategoriesProvider"
            android:authorities="com.zaks.ayala.provider.categories" />
        <provider
            android:name=".data.contentproviders.DealsProvider"
            android:authorities="com.zaks.ayala.provider.deals" />
        <provider
            android:name=".data.contentproviders.SuppliersProvider"
            android:authorities="com.zaks.ayala.provider.suppliers" />

        <activity
            android:name=".ui.AddDealActivity"
            android:label="@string/title_activity_add_deal"
            android:parentActivityName=".ui.SupplierDealsListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.SupplierDealsListActivity"
            android:label="@string/title_activity_supplier_deals_list"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.EditDealActivity"
            android:label="@string/title_activity_edit_deal"
            android:parentActivityName=".ui.SupplierDealsListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.DealsActivity"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:parentActivityName=".ui.MainActivity"
            android:name=".ui.SupplierDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".bl.services.SupplierIntentService"
            android:exported="false" />
        <service
            android:name=".bl.services.DealIntentService"
            android:exported="false" />

        <service android:name=".bl.services.DealsNotificationsService"
            android:permission="com.google.android.gms.permission.BIND_NETWORK_TASK_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.gcm.ACTION_TASK_READY"/>
            </intent-filter>
        </service>
    </application>

</manifest>